/**
 * Tagline marquee — bold uppercase scrolling strip.
 *
 * The marquee is now implemented as a pure CSS translateX keyframe loop
 * (see `.tagline-marquee` styles in main.css), so no JS animation is
 * required. This module is kept for structural consistency with the
 * section layout; it intentionally does nothing.
 */
export default function initQuoteMarquee() {
  return null;
}
