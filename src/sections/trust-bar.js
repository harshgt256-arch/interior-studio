/**
 * Trust bar — awards / press recognition strip.
 *
 * The marquee is now implemented as a pure CSS translateX keyframe loop
 * (see `.press-strip` styles in main.css), so no JS animation is required.
 * This module is kept for structural consistency with the section layout;
 * it intentionally does nothing.
 */
export default function initTrustBar() {
  return null;
}
